# Shared Cases

This directory is reserved for parity datasets that should be runnable in both:

- the standalone package build rooted in `spread_engine_extract/`
- the LibreOffice-integrated `spreadsheetengine` path

Phase A only establishes the directory structure. Shared datasets should be
added as more extracted helpers gain standalone coverage.
