# Computational Substrate Off-Sheet Gap Insertion Surface Plan

Status: in progress focused pass for the retained direct off-sheet
gap-closing insertion surface

Phase status:

- Phase 1 in progress: freeze the bounded host-shape target and proof ladder
- Phase 2 pending: probe the missing live authority surface
- Phase 3 pending: land the smallest exact runtime or proof change
- Phase 4 pending: close the pass as admission or retained defer

## Purpose

Clear the last retained one-consumer-sheet direct off-sheet widening
question:

- same-workbook direct off-sheet `SetFormula`
- touched cell is the blank gap between two shareable same-column groups
- off-sheet consumer is bounded to one sheet
- live Calc merges the after-topology
- the facade mutation-family surface remains `None`

The goal of this pass is not to invent a new synthetic family. It is to
decide whether that exact live host-shaped lane can now be admitted on exact
authority, lifecycle, and mutation-entry carry-through.

## Bounded Target

This pass is limited to the narrow direct off-sheet surface where:

- there are exactly two before-groups on the mutation sheet
- the touched address is not shared before
- the touched address becomes shared after
- the after-topology becomes one merged shareable group
- there is exactly one off-sheet consumer sheet
- there are no named ranges on the mutation boundary

This pass excludes:

- named-range-combined off-sheet widening
- replacement attempts, which already normalize to admitted direct
  `Regroup`
- multi-consumer-sheet or workbook-wide off-sheet behavior
- structural off-sheet widening

## Why This Is The Right Next Pass

The repo already proves three useful things:

- engine-only exactness for the bounded direct off-sheet merge-shaped
  topology already closes
- live lifecycle and mutation-entry carry-through buckets already exist for
  the same bounded surface
- the retained documentation blocker is now specifically the missing stable
  direct authority closeout for the host-uncategorized gap-closing
  insertion lane

That means this is a high-leverage bounded pass: either the missing live
authority proof closes and the blocker disappears, or the pass isolates the
exact remaining mismatch without reopening broader off-sheet work.

## Proof Ladder

The pass should close on the following ladder:

1. Freeze the exact live host shape in workbook-facade proof.
2. Add bounded live authority proof for the direct off-sheet gap-closing
   insertion surface.
3. Reconfirm lifecycle and mutation-entry proof on the same surface.
4. If authority does not close, patch only the smallest exact seam and rerun
   the bounded ladder.
5. Close the docs and current-state boundary as either admitted or retained
   defer.

## Success Condition

This pass is successful if one of the following becomes true:

- the bounded direct off-sheet gap-closing insertion host shape is admitted
  exactly on authority, lifecycle, and mutation-entry, even if the facade
  mutation-family classifier remains `None`
- or the pass closes with a precise retained-defer reason backed by live
  authority proof rather than roadmap inference
