# Computational Substrate Shared-Group Named-Range Non-Scalar Member-Exit Scenario Matrix

Status: frozen scenario matrix for bounded named-range-combined non-scalar
member-exit closeout

## Admit Bucket

- same-sheet shareable named-range-combined `SetFormula`
  `MemberExit` where the touched member becomes ordinary formula text and
  the surviving group remains one contiguous run

## Retained Defer Bucket

- same-sheet shareable named-range-combined `ClearCell`
  `MemberExit` on the same bounded surface
  reason: live Calc after-state closes on the broadcaster surface, but the
  authority planner still predicts different broadcaster nodes and graph
  edges, so the lane is not exact yet

## Still-Rejected Frontier Bucket

- named-range-combined regroup
- named-range-combined merge
- named-range-combined multi-group collapse
- repair-sensitive normalization
- off-sheet shared-group consumers
