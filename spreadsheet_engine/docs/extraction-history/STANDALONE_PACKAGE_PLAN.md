# Standalone Package Plan

The standalone package roadmap has been consolidated into:

- `spreadsheet_engine/docs/extraction-history/CALC_ENGINE_EXTRACTION_PLAN.md`

That document is now the single source of truth for both:

- the remaining Calc extraction work
- the remaining standalone package work

The consolidated plan keeps the two tracks phased together so that every new
engine slice is considered from both angles:

- how Calc consumes it
- how standalone builds and tests consume it
